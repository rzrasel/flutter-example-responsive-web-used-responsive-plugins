# flutter-example-responsive-web-used-responsive-plugins

Flutter Example Responsive Web Used Responsive Plugins

## 🧰 Git Commands

```bash
git init
git remote add origin https://github.com/rzrasel/flutter-example-responsive-web-used-responsive-plugins.git
git remote -v
git fetch && git checkout master
git add .
git commit -m "Add Readme & Git Commit File"
git pull
git push --all
git status
git status
```

### 🧩 Git Rebase Squash Interactive

```bash
git rebase -i HEAD~2
i
[delete word: pick [make it] squash/s]
esc:wq↵

i
[change commit comment by #]
esc:wq↵

------------------------------------

git rebase -i 4daac6b7
i
[delete word: pick [make it] squash/s]
esc:wq↵

i
[change commit comment by #]
esc:wq↵

git push --force
//git push -f --set-upstream origin master

------------------------------------

git rebase -i --root
i
[delete word: pick [make it] squash/s]
esc:wq↵

i
[change commit comment by #]
esc:wq↵

git push --force

//git push -f --set-upstream origin master
```

```PHP_DATE_TIME
echo date("D", (time() + 6 * 60 * 60)) . "day " . date("F j, Y, G:i:s", (time() + 6 * 60 * 60));
```

[Learn Git Squash in 3 minutes // explained with live animations!](https://youtu.be/V5KrD7CmO4o)

- provide README.md download
- write all in one code editor for copy all
- all in one code snippet